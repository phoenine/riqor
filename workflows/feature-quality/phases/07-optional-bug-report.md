# Feature Testing — Optional Bug Report

| | |
|---|---|
| **目标** | 将执行失败转为 bug 草稿或 测试管理平台操作（可选） |
| **输入** | 执行记录、失败证据、预期行为上下文 |
| **产出** | `bug_report` 或 测试管理平台操作结果；或 skip |

**Skills:** `reporting`; Project Profile integration Skill when writing an external platform

**Confirmation before:** test-management bug create/update.

**Machine:** optional; `bug_report` or skip — `tools/stage_gate.py`

**Prev → Next:** Optional Case Execute → Optional Test Report
