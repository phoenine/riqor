# 需求说明书：<需求名称>

## 基本信息

| 字段 | 内容 |
|------|------|
| 项目与适用 Track | <project_id> / <track> |
| 产品 / 系统 |  |
| 模块 |  |
| 需求来源 | PRD / 文档平台 / 测试管理平台 / MR / 用户描述 |
| 需求优先级 |  |
| 适用版本 |  |
| 负责人 / 相关方 |  |

## 摘要

用 3-5 句话说明这个需求解决什么问题、面向谁、核心变化是什么。

## 来源与证据

### 业务来源

| 来源类型 | 链接 / ID | 说明 |
|----------|-----------|------|
|  |  |  |

### 实现证据

| 仓库 / 系统 | 分支 / commit / 文件 | 说明 |
|-------------|----------------------|------|
|  |  |  |

### 未确认来源

- 

## 领域术语

用于记录会影响理解、测试设计或知识库沉淀的术语、指标、状态、表名、枚举、缩写。若术语属于可复用领域知识，但知识库中不存在或口径变化，应在本阶段记录待确认问题，并在 run state 中登记知识库补充 proposal。

| 术语 | 含义 | 来源 | 是否建议补充知识库 |
|------|------|------|--------------------|
|  |  |  | 是 / 否 / 待确认 |

### 知识沉淀候选（可选）

只列出跨需求仍然成立的术语、业务规则、公式、实体关系或长期契约。版本范围、
Feature Flag、临时方案、当前 Bug 证据、环境信息和待确认推断不得进入候选。
候选只登记为 `proposed`，不能因为需求文档通过 Gate 而自动写入知识库。

| 候选标题 | 类型 | 目标路径 | 稳定性依据 | 来源需求项 |
|----------|------|----------|------------|------------|
|  | term / business_rule / user_flow / interface / data_model / ... |  |  |  |

## 变更说明

### 问题背景

说明改造前是什么状态、用户遇到什么业务问题、为什么需要做。这里应保留足够工程上下文，在没有详细设计说明书时能支撑研发和测试理解。

### 目标结果

列出需求完成后要达成的业务结果。

### 非目标

明确本需求不解决什么，避免范围膨胀。

### 视图 / 业务层级关系

说明功能位于哪个业务层级，以及上下游页面或模块关系。

```text
工厂 → 区域 → 产线 → 工位 → 设备组
```

### 改造前后对比

| 项目 | 改造前 | 改造后 |
|------|--------|--------|
| 数据来源 |  |  |
| 计算位置 |  |  |
| 性能 |  |  |
| 指标能力 |  |  |
| 用户操作 |  |  |

### 数据流架构

用文字、表格或代码块描述 UI、API、数据表、管道之间的链路。跨前端、后端 API、数据表、数据管道的需求必须写清每一层输入、处理、输出和职责边界。

```text
[数据源]
  → [处理 / 聚合]
  → [API]
  → [前端视图]
```

### 关键计算口径 / 公式

记录核心公式、状态判断、优先级、回退链、默认值等。

| 指标 / 规则 | 口径 |
|-------------|------|
|  |  |

### 示例介绍

用一个典型业务场景解释需求入口、改造前后差异、目标效果和关键数据。示例应尽量包含可验证的数据和预期结果。

### 根因分析 / 为什么需要改造

说明旧链路为什么会导致问题，例如缺少持久化层、口径分散、性能瓶颈、配置缺失、数据重复计算等。

### 核心模块变更

| 模块 | 变更 | 说明 |
|------|------|------|
| 前端 |  |  |
| 后端 API |  |  |
| 数据管道 |  |  |
| 数据表 |  |  |
| 智能体 / 其他 |  |  |

### 实现差异 / 待确认设计点

集中列出需求、设计、代码之间不一致或需要确认的地方。

| 编号 | 差异 / 问题 | 影响 | 建议确认人 |
|------|-------------|------|------------|
| D-001 |  |  |  |

## 用户故事 / 场景卡片

### US-001 <场景名称>

**作为** <角色>  
**我希望** <完成的动作>  
**以便** <业务价值>

**入口**
- 

**前置条件**
- 

**主流程**
1. 
2. 
3. 

**异常 / 分支流程**
- 

**验收标准**
- Given 
- When 
- Then 

## 范围

### 范围内

### 范围外

## 需求明细

### Source Fidelity 与原子化规则

- 一个 Requirement Spec 可以包含多个 Atomic Requirement；不要强制一个上游 Feature / F-SYS 只对应一个 REQ。
- 每个 REQ 只描述一个可独立判定的义务或用户可观察结果。不同触发条件、入口、状态、分支、结果、来源或确认状态应拆分。
- `source_explicit` 表示权威需求来源明确陈述；`user_confirmed` 表示获得明确确认；`assumption` 表示尚未建立的假设，只能保持 `pending` 并关联待确认问题。
- 代码、配置和运行现象属于实现证据；测试边界、组合和负向路径属于测试设计推导。二者都不得在未经确认时冒充产品要求。
- 使用连续编号 `REQ-001`、`REQ-002`……；上游 Feature ID 写入来源定位和追溯表，不使用 `REQ-001-01`。

### REQ-001 <功能点名称>

**需求陈述**：<一个可独立判定的义务或用户可观察结果>

**依据类型**：<source_explicit | user_confirmed | assumption>

**确认状态**：<confirmed | pending | conflict>

**来源定位**：<来源 ID、文档章节、需求记录或用户确认；assumption 填关联的 Q-ID>

**优先级**：Must / Should / Could

**验收口径**：<可独立判断通过或失败的业务结果>

## 业务规则与边界条件

| 规则 ID | 规则 | 说明 |
|---------|------|------|
| BR-001 |  |  |

## UI 影响

| 页面 / 组件 | 变化 | 用户可见行为 |
|-------------|------|--------------|
| 无 | 无 | 无 |

## API 契约

| 接口 | 方法 | 参数 | 响应 | 说明 |
|------|------|------|------|------|
| 无 | 无 | 无 | 无 | 无 |

## 数据 / 口径 / 管道影响

| 对象 | 类型 | 影响 |
|------|------|------|
| 无 | 无 | 无 |

## 兼容性与异常状态

- 空数据：
- 权限不足：
- 配置缺失：
- 历史数据缺失：
- 新旧逻辑兼容：

## 待确认问题

| 编号 | 问题 | 影响 | 建议确认人 |
|------|------|------|------------|
| Q-001 |  |  |  |

## 使用的知识库

| 知识文件 | 使用结论 |
|----------|----------|
|  |  |

## 可追溯关系

| 上游 Feature / 精确来源 | 用户故事 | Atomic Requirement | 业务规则 | 后续风险 / 测试点 | 覆盖状态 |
|--------------------------|----------|--------------------|----------|-------------------|----------|
|  | US-001 | REQ-001 | BR-001 | 待生成 | 待覆盖 / 已覆盖 / Gap |
