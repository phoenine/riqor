# 回归计划

## 摘要

## 回归策略

策略：`smoke` / `targeted` / `module` / `cross_module` / `full`

## 决策路径

路径：`execute_existing` / `supplement_cases` / `generate_automation` / `no_executable_regression`

## 执行计划

| 项 | 内容 |
|---|---|
| 环境 | |
| 账号 / 数据 | |
| 已有用例 / 自动化 | |
| 补充用例引用 | |

## 补充用例计划

当 `decision_path` 为 `supplement_cases` 时，补充用例必须单独写入
`<artifact-root>/<scope>/<track>/test-cases/`（`test_cases` 产物），本段只记录引用与范围。

## 自动化生成计划

## 确认项

| 动作 | 是否需要确认 | 状态 |
|---|---|---|

## 可追溯关系
