[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{PROJECT_NAME}}"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8,<9",
  "rigorpath-api-test=={{RUNTIME_VERSION}}",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
