[project]
name = "{{PROJECT_NAME}}"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8,<9",
  "{{RUNTIME_DEPENDENCY}}",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
package = false
